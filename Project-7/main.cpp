// Simulation Requirements
//======================================================================================================================

// * Create a VehicleState

// * Create a CruiseController with chosen PID gains

// * Run a simulation loop (e.g. 40–60 time steps)

// * At each step:
    // * Call computeControl(current_speed, target_speed)

    // * Update the vehicle with the returned acceleration

    // * (Strongly recommended) Print speed and error each step so you can see the controller behavior

// * At the end, print a summary

// You should experiment with different gains (kp, ki, kd) and observe how the vehicle behaves (overshoot, oscillation, steady-state error, etc.). This is where the real learning happens.

int main() {
    return 0;
}