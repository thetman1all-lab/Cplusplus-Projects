#include <opencv2/opencv.hpp>
#include <iostream>


int main() {
    
    // cv::Mat image = cv::imread("data/CyAUG-Dataset/Alluvial_Soil/1.jpg", cv::IMREAD_GRAYSCALE);

    // // Check if the image was loaded successfully
    // if (image.empty()) {
    //     std::cout << "Could not open or find the image\n";
    //     return -1;
    // }

    // // Display the image in a window
    // cv::imshow("Image Display", image);

    // // Wait for a key press indefinitely to keep the window open
    // cv::waitKey(0);

    // // Destroy the window
    // cv::destroyWindow("Image Display");

    return 0;
}